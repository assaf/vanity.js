#!coffee


# Use separate index for searching.
elasticsearch:
  hostname:   "localhost"
  port:       9200
  index:      "test-vanity"

# Redis server and prefix
redis:
  hostname:   "localhost"
  port:       6379
  prefix:     "test-vanity"
